export enum RiverType {
  Rainfall = "pch",
  WaterLevel = "tma",
  Climate = "iklim",
}

export enum RiverObservation {
  Manual = "manual",
  Telemetry = "telemetry",
}

export enum UserType {
  Admin = "admin",
  User = "user",
  Operator = "operator",
}
